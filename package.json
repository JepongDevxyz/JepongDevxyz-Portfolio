{
  "name": "jepongdevxyz-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check app.js && node --check scene.js && node --check projects.js && node scripts/verify.mjs",
    "verify": "npm run check && npm test"
  }
}
